import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    iconContainer: {
        height: 48,
        width: 48,
        alignItems: 'center',
        justifyContent: 'center'
    }
});