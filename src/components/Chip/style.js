import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    button: {
        paddingHorizontal: 16,
        paddingVertical: 8,
        borderRadius: 20,
    },
    text: {
        color: '#fff',
        fontSize: 17,
        fontWeight: '700',
        lineHeight: 20,
        textAlign: 'center',
    },
});