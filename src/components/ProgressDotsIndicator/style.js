import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
        flex: 1,
        height: '100%',
        flexDirection: 'row',
        alignSelf: 'center',
        justifyContent: 'center',
    },
    indicator: {
        borderRadius: 100,
    }});